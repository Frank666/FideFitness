import React from "react";
import { Button } from "react-bootstrap";

class myButton extends React.Component {
  render() {
    return <Button variant="primary">Lets WOD!</Button>;
  }
}

export default myButton;
